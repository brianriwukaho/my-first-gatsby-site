import * as React from "react";
import Layout from "../components/layout";

const BlogPage = () => {
  return (
    <Layout pageTitle="Blog">
      <p>Hi there! Blog Soon!</p>
    </Layout>
  );
};

export default BlogPage;
